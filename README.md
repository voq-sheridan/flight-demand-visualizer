# flight-demand-visualizer
A dynamic data visualization built with Observable Framework and D3.js that displays real-time and upcoming flight activity to support airport HR staffing decisions.

If live AeroDataBox data is temporarily unavailable (for example, API quota exhaustion), the build now publishes a clearly marked demo fallback snapshot so the dashboard remains visible instead of rendering empty.
